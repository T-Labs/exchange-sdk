namespace TLabs.ExchangeSdk.Verification
{
    /// <summary>
    /// Relative paths to stock-verification via Gateway (upstream <c>/api/verification/{{everything}}</c>).
    /// </summary>
    public static class VerificationGatewayRoutes
    {
        /// <summary>POST Sumsub SDK access token.</summary>
        public const string SumsubAccessToken = "verification/sumsub/access-token";
    }
}
